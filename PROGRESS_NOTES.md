# V0.3.4 进度快照 (中断恢复点)

生成时间：本次会话恢复后立即打包，测试全绿状态。

## 已完成

1. **Quiescence Search 核心实现** (`src/alphazetacchess/engine/search.py`)
   - `_quiescence()` 方法：只搜索"吃子"和"被将军时的应将"两类走法，直到局面
     "安静"为止，解决地平线效应（fixed-depth 搜索恰好停在一次吃子交换中间，
     误判局势）。
   - fail-soft 负极大值 + stand-pat 剪枝。
   - 被将军时不做 stand-pat，强制搜完所有应将走法（对应 roadmap 里的
     "check extensions"）。
   - `quiescence_max_ply` 安全深度上限，防止病态的长将杀链条无限递归。
   - 部分接入置换表（安静局面 / beta 裁剪结果会缓存；命中安全上限时不缓存，
     因为该值依赖调用时的 qply 预算，不是与局面本身绑定的通用值）。
   - `_negamax` 的 `depth == 0` 分支改为调用 `_quiescence` 而非直接 `evaluate()`。
   - `__init__` 新增 `use_quiescence`、`quiescence_max_ply` 开关，遵循项目一贯的
     "每个优化都能单独开关、便于 A/B 对比"的模式。

2. **修复了两个测试局面的构造缺陷**（`tests/test_search.py`、
   `tests/test_search_v031.py`）：
   - 反复踩到同一个坑：手搭最小测试局面时，如果红黑双方的将都放在默认的
     第4列，而中间又没有任何棋子（真实对局靠中路兵卒挡着），要么直接构成
     "飞将"违规，要么让某一方从走棋前就已经被将军——这会让测试意图完全跑偏。
   - `test_engine_avoids_hanging_its_own_rook_when_it_can_see_the_recapture`：
     原局面里光将（无仕象保护）在这么少子力的残局下极易撞上真实的杀棋，
     旧版搜索因为只看2层看不到，新版静态搜索能看到，导致原测试的"更安全的
     走法"假设本身就是错的。现在补全了双方仕象，让"是否送车"成为局面里
     唯一起决定作用的因素。
   - `test_iterative_deepening_matches_fixed_depth`：原来断言"迭代加深"和
     "固定深度"必须选出完全相同的最佳着法，但当有多个着法评分相同（真实平
     局）时，两种走法排序不同会导致"先遇到谁就选谁"的结果不同——这不是bug，
     是过强的断言。已改为只验证分数一致（这才是真正有保证的不变量），并
     加了详细注释解释为什么不能断言走法完全相同。

3. **完整测试套件**：32 个测试全部通过（含 4 个上面提到的修复/新增）。

## 还没做完（下次继续的起点）

- [ ] 专门给 Quiescence Search 写正确性测试（`tests/test_search_v034.py`，
      还没创建文件）。已经在会话里手工验证过一个"地平线效应"演示局面
      （红车吃炮看起来+15分，但静态搜索发现黑车能反吃，正确评为-920分），
      这个局面可以直接搬进正式测试。
- [ ] `docs/v0.3.4.md` 验收文档还没写（参照 `docs/v0.3.3.md` 的格式：
      目标/设计/范围/验收标准/实测数据/已知局限/下一步）。
- [ ] 有 vs 无 Quiescence 的性能基准测试还没跑（对照 V0.3.1-3.3 的做法，
      需要在几个参考局面上实测 depth 2/3 的节点数、耗时、分数变化）。
- [ ] `roadmap.md` 里 V0.3.4 那一节还没有更新为 COMPLETE + 实测数据。
- [ ] `main.py` 顶部注释还没更新到 V0.3.4。

## 如果沙盒没了，怎么从这份文件继续

这个 zip 里的 `src/alphazetacchess/engine/search.py` 就是当前的完整实现，
可以直接覆盖你仓库里的对应文件，然后本地跑 `pytest` 验证仍然全绿，就是
在这个基础上继续往下做上面"还没做完"的部分。
